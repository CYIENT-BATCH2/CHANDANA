#include <stdio.h>
int main() {
    float r,area;
    printf("enter radius of the garden is:");
    scanf("%f",&r);
    area=3.14*r*r;
    printf("area of the garden is %f",area);
    return 0;
}